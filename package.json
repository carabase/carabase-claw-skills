{
  "name": "carabase-claw-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Carabase MCP skills and Claude Desktop integration",
  "type": "module",
  "scripts": {
    "build:claude": "npx tsup src/claude-mcp.ts --format cjs --clean --outDir dist"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.2"
  }
}
